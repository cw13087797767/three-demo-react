import React from 'react'
import { connect } from 'dva'

const webgl_animation_cloth: React.FC = (props:any) => {
    return(
        <div>webgl_animation_cloth</div>
    )
}

export default connect()(webgl_animation_cloth)